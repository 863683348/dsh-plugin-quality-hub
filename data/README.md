# data

Generated by `scripts/fetch-data.mjs` (committed to `dist/` for static deploy; catalog.json itself is gitignored and regenerated weekly).

- `catalog.json` — full scored catalog (top 100 by stars + npm probes + security signals)
- `meta.json` — generation metadata (timestamp, counts)
